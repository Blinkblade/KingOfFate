# ============================================================================
# KingOfFate — Base Fighter Template : 受击方状态（hits.zss）
#
# 当一个角色**被别人打中**时，引擎不会自动处理细节 —— 需要攻击方在自己的
# hitDef 里指定对手进入哪个状态（p2stateno），或者由受击方在这里定义。
#
# 命名约定：KFM 把"某招式对应的受击状态"放在贴近该招式编号的号段里：
#     招式 200（站立轻拳）  →  对手进入普通受击状态（公共状态，无需定义）
#     招式 800/810（投技）  →  对手进入 820 / 821
#     招式 1000-1020（金刚掌）→  对手进入 1025 - 1028
#
# 也就是说：**普通技靠公共受击状态，投技/需要特殊表现的招式才需要在这里定义。**
#
# P2 起点：本文件为空骨架 —— 普通技的受击完全由 common1.cns.zss 提供。
#
# 依据：docs/ikemen_character_architecture.md §2.1 / §4.1
# 实测对照：game/chars/p1_kfm_zss_lab/hits.zss（820 / 821 / 1025-1028）
# ============================================================================


# ============================================================================
# 投技受击状态样板（未启用）
#
# 投技的受击方需要被"抓住"而不是被击退，所以必须有独立状态。
# 关键点：用 bindToParent / target 把对手锁在攻击方身上，然后用 posSet
# 把他摆到正确位置。
# ============================================================================
#[StateDef 820;
#type: S; movetype: H; physics: S;
#anim: 820; ctrl: 0;
#velset: 0, 0;]

# 被抓者不可操作、不可被其他攻击打到
#notHitBy{value: SCA, AA, AP, AT; time: 1}

# ...按投技动画摆位...

#call EndState(0);
